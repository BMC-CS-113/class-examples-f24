Keep track of the current value of the hand
check the value of the current hand, then act accordingly
act according: print blackjack, print stand, generate a new card
generate a new random number/card
decide whether to generate a new card

Verbs:
  - generate a new card
  - decide whether to generate
  - keep track of current value/update the current value
  - check the value of current hand
  - act acordingly

Nouns:
  - current value
  - new card

Use variables to store the nouns
create methods for each verb 

COmbe up with algorithm 

1. Create a hand: 
   1.1 Generate the first card
   1.2 Update the value of the hand
   1.3 Generate the second card
   1.4 Update the value of the hand
2. Check the value of the current hand:
  2.1 if currentHandValue > 21:
    2.1.1 print losing message 
    2.1.2 and end
  2.2 if currentHandValue > 16:
    2.2.1 print stand and end
  2.3 otherwise:
    2.3.1 print hit
    2.3.2 generate another card
    2.3.3 updae teh value of the current hand
    2.3.4 go back to 2


